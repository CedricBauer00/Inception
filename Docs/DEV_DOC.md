#USER Documentation

The services can be viewed in the dockercompose ( maybe there is cpmmadn that shows all services right away)

Start the project with make and stop the project with make fclean

You can access the Website by a GUI Ubuntu VM via the Brwoser, entering 'localhost'

The credentials are located in the secrets folder

You can check the services by doing 'docker ps' after running 'make'